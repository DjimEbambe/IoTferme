<!DOCTYPE html>
<html lang="fr" data-bs-theme="dark">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Inscription — FarmStack</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      background: radial-gradient(circle at top, rgba(41, 60, 90, 0.65), #0f111a 60%);
      font-family: 'Poppins', system-ui, sans-serif;
    }
    .auth-card {
      background: rgba(14, 17, 32, 0.92);
      border-radius: 1.5rem;
      border: 1px solid rgba(255, 255, 255, 0.06);
      box-shadow: 0 30px 60px rgba(6, 9, 20, 0.55);
      max-width: 520px;
      width: 100%;
    }
  </style>
</head>
<body class="d-flex align-items-center justify-content-center px-3 py-4">
  <form method="post" action="/signup" class="auth-card p-4 p-lg-5 text-light">
    <div class="text-center mb-4">
      <div class="fs-2">🐣</div>
      <h1 class="h4 mb-1">Créer mon compte</h1>
      <p class="text-secondary mb-0">Accès unifié ERP • Twin • IoT</p>
    </div>

    <% if (successMessage) { %>
    <div class="alert alert-success" role="alert">
      <i class="bi bi-check-circle me-2"></i><%= successMessage %>
    </div>
    <% } %>
    <% if (errors && errors.length) { %>
    <div class="alert alert-warning" role="alert">
      <ul class="mb-0 ps-3">
        <% errors.forEach((err) => { %><li><%= err %></li><% }) %>
      </ul>
    </div>
    <% } %>

    <input type="hidden" name="_csrf" value="<%= csrfToken %>">

    <div class="mb-3">
      <label class="form-label">Nom complet</label>
      <input type="text" class="form-control" name="displayName" placeholder="Opérateur Kinshasa" value="<%= formData?.displayName || '' %>">
    </div>
    <div class="row g-3">
      <div class="col-md-6">
        <label class="form-label">Email</label>
        <input type="email" class="form-control" name="email" placeholder="vous@ferme.cd" value="<%= formData?.email || '' %>">
      </div>
      <div class="col-md-6">
        <label class="form-label">Téléphone</label>
        <input type="tel" class="form-control" name="phone" placeholder="+243 89 99 00 000" value="<%= formData?.phone || '' %>">
        <small class="text-secondary">Optionnel mais recommandé.</small>
      </div>
    </div>

    <div class="row g-3 mt-1">
      <div class="col-md-6">
        <label class="form-label">Mot de passe</label>
        <input type="password" class="form-control" name="password" required>
        <small class="text-secondary">8+ caractères avec majuscules et chiffres.</small>
      </div>
      <div class="col-md-6">
        <label class="form-label">Confirmation</label>
        <input type="password" class="form-control" name="confirmPassword" required>
      </div>
    </div>

    <button class="btn btn-success w-100 mt-4" type="submit">Créer le compte</button>

    <div class="d-flex justify-content-between text-secondary small mt-3">
      <span>Déjà enregistré ?</span>
      <a href="/login" class="text-decoration-none text-light">Se connecter</a>
    </div>
  </form>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
